---
- name: Deploy app server
  hosts: all
  become: true
  tasks: 
    - name: Install apache2 package in ubuntu server
      apt:
        name: apache2
        state: present
      when: ansible_distribution == "Ubuntu"


